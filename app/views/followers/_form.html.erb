<%= form_with(model: friend, local: true) do |form| %>
  <% if follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(follow.errors.count, "error") %> prohibited this friend from being saved:</h2>

      <ul>
      <% follow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :follow_id %>
    <%= form.text_field :follow_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
